# Echo Classic 3.3.0 browser matrix

Status: active release gate. Browser: Google Chrome against the deployed LMS. A failed
row blocks publication and returns the current idea to implementation.

| Feature / state | Action and expected visible result | Matrix | Engineering checks | User check | Result / evidence |
|---|---|---|---|---|---|
| Advanced shell | Open Settings, then Advanced LMS settings; Echo shell, rail, search and content load without overflow | Light · wide | Page overflow 0; iframe matches content width | Navigation is clear | PASS · live Chrome `setvis-shell-wide-20260816` |
| File Types conversion | Open File Types; native decoder selects remain usable in an Echo table | Light · wide | NEW present; native selects retained; no page overflow | Rows and controls are legible | PASS · live Chrome `setvis-filetypes-wide-20260816` |
| Artist enrichment | Open The Beatles from My Music after upgrading a browser with the old cache | Light · wide | Plain biography, credited image, provenance, retrieval time, NEW, local albums unchanged | Collapsed panel 325px; Read biography/Show less works; no raw tags; overflow 0 | PASS · live Chrome asset `1786898731` + cache v2 deploy |
| Scan completion | Advanced LMS Settings → Information | Light · wide | Ten semantic gauges, NEW and empty persistent error journal | No dotted native bars or nested frame; overflow 0 | PASS · live Chrome 2026-08-16 |
| Scan theme integrity | Repeat Information page in Dark and Legacy | Dark · Legacy · wide | Echo tokens propagate to inherited LMS content | Gauges 10; journal and NEW present; overflow 0 in both | PASS · live Chrome 2026-08-16 |
| File Types responsive | Resize narrow; table reflows instead of creating a framed inner viewport | Light · narrow | Effective viewport 537×1000; page overflow 0; row grid `1fr`; parent scroll width equals width | Controls remain reachable | PASS · live Chrome `setvis-filetypes-narrow-20260816` |
| Media Scan Details | Open Information; completed scan renders semantic gauges without legacy dots or nested frames | Light · narrow | 10 named progressbars; no dotted native marks | Scan stages and completion are understandable | PASS · corrected deployment `1786889460` |
| Advanced native tables | Open Basic Settings in Dark; inherited tables follow Echo theme | Dark · wide | Page overflow 0; inherited cells `rgb(16,16,18)`; menu/autocomplete transparent | Media folders no longer appear as a foreign panel | PASS · live Chrome `setvis-dark-basic-corrected-20260816` |
| Dark scan details | Open Information after corrective deploy | Dark · wide | 10 semantic gauges; no raw inherited background or nested frames | Scan stages are clean and readable | PASS · live Chrome `setvis-dark-scan-corrected-20260816` |
| Legacy inherited Settings | Visit table, form, responsive long-content and gauge families | Legacy · wide/narrow | Basic controls, File Types reflow and scan roles; visual scroll ownership | All representative inherited families look native to Legacy | PASS · live Chrome `setvis-legacy-wide-narrow-20260816` |
| Main Settings controls | Exercise appearance, theme, accent, font, player layout, crossfade and replay-gain controls | All themes · relevant widths | Keyboard/focus, value/persistence, disabled/fallback states, console | Every changed control works through visible UI | PASS · final deploy asset `1786908383`; hard reload; named/selected/disabled controls and Replay gain Album read-back pass. EN/PT Language heading/help, wide/narrow overflow 0, console 0; language restored to English |
| Track information and lyrics | Open tracks with and without lyrics/information | All themes · wide/narrow | Present/absent/error states; back navigation; stale response handling | Information is understandable and navigation stable | PASS/PARTIAL · corrected deploy shows FLAC, focus on Done, Back/close path, narrow overflow 0 and console 0. Current library track had no lyrics; real positive lyrics tag remains unavailable on this server, guarded by focused mapper/component tests |
| OPML browsing | Exercise pagination, actions, back/restore and end state | Light · wide/narrow live | Qobuz Releases appended 100→200→300→400→434; appended row opened; final end state; document overflow 0; console errors 0 | Every row remains actionable and the complete list is reachable | PASS · deployed asset `1786892228`; Chrome live 2026-08-16 |
| Player/queue/capabilities | Switch or reconnect players; inspect queue identity, ratings and capability fallbacks | All themes · wide/narrow | Repeated/stale responses, disabled controls, persistence, console | Playback UI remains coherent | PASS WITH ENVIRONMENT LIMIT · single available SqueezeLite selected coherently; player sheet, fixed/LMS volume state, rating, queue identity, 6-row ordering/removal controls and narrow overflow pass with console 0. Multi-player switch/reconnect cannot be reproduced with only one live player; stale writes covered by focused tests |

The release-wide settled pass must replace every remaining `PENDING` row with evidence.
The browser session is kept singular to minimize usage; one compact read-only inventory
pass supplied the release feature list without duplicating screenshots or interaction.

SETVIS-01 note: Chrome produced no warning/error log entries. A single monolithic sweep
of every rail destination exceeded the browser execution window and was not repeated;
the settled gate instead uses bounded semantic checks for every materially distinct DOM
family, as required by the loop, without weakening the release-wide feature inventory.

Full-loop finding 2026-08-16: the live Qobuz response stores common row actions under
`result.base.actions` and row identity under `item.params`. The deployed API ignored that
contract, leaving the first page inert and treating page two as having no usable items.
The local correction expands shared actions before mapping rows and has a focused
real-contract regression. Retest on asset `1786892228` loaded all 434 releases, kept
page-two rows actionable and produced the correct terminal state with no console errors.

Corrective rundown 2026-08-16: the release-wide Chrome pass exposed `FLC` in Track
information while the surrounding UI correctly displayed `FLAC`. Independent code
review also found stale player-setting reads, stale artist/album enrichment responses,
a disappearing plugin-absent album panel and dynamic strings that bypassed explicit
translation. Focused tests and validation pass locally; these rows remain non-final
until the corrected asset is deployed and the failed matrix points are repeated.

Final strings correction 2026-08-16: restore point `20260816-171020`, installed
3.3.0, HTTP 200 and asset `1786908383`. Chrome hard reload confirmed `IDIOMA` plus
the complete Portuguese reload explanation, document overflow 0 and console errors 0;
the test language was restored to English.

Release artifact rebuilt after the final live pass: `EchoClassic-3.3.0.zip`, 34 files,
266033 bytes, tree-identical, SHA-1 `a06ef544a22fa7344449d971067ed56a0bc6608c`;
`repo.xml` SHA and all three version declarations agree. Publication remains user-owned.
